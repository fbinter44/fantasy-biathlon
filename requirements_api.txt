# Dépendances FastAPI (en plus de requirements.txt existant)
fastapi
uvicorn[standard]
pydantic-settings
pydantic[email]
python-jose[cryptography]   # JWT
python-multipart             # pour OAuth2PasswordBearer (form login)
