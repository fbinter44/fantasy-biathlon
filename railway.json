{
  "$schema": "https://schema.railway.app/config.json",
  "build": {
    "buildCommand": "pip install -r requirements_api.txt"
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
